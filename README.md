# DayWise
DayWise - a single-file calendar web app with month, week, and day views, color-coded categories, search, dark mode, and JSON import/export. Events save to localStorage, so no server or sign-up is needed. Works on desktop and installs to the iOS home screen.
